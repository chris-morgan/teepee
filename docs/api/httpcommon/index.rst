:crate:`httpcommon`: functionality shared between client and server
===================================================================

.. crate:: httpcommon

.. note::

   Your own code should never reference :crate:`httpcommon`; everything in it
   is exposed in the relevant crates such as :crate:`httpc` and :crate:`httpd`.

.. toctree::

   status
